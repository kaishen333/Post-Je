const express = require('express');
const router = express.Router();

const ProductService = require('../services/dropOffCentersService');
const ProductServiceInstance = new ProductService();

module.exports = (app) => {

}